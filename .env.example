# Production: Set to your live API endpoint (e.g., https://api.yourdomain.com/api or /api if using reverse proxy)
VITE_API_URL=http://localhost:8000/api
VITE_APP_NAME=ResellSeba

# Optional: Set to true ONLY if you want to run offline mock mode without backend
VITE_USE_MOCK=false
